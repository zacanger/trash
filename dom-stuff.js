const scrollTop = () =>
  global.scrollTo(0, 0)

const getWidth = () =>
  global.innerWidth ||
  global.document.documentElement.clientWidth

const getHeight = () =>
  global.innerHeight ||
  global.document.documentElement.clientHeight
